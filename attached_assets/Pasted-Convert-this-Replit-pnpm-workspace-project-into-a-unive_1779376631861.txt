Convert this Replit pnpm workspace project into a universal hosting-compatible production-ready website that can run on both Node.js hosting and standard shared hosting environments.

MAIN GOAL:
Create a stable, lightweight version of the project that works on:
- Render
- Railway
- VPS
- cPanel Node.js hosting
- Traditional shared hosting (HTML fallback)

TRANSFORMATION REQUIREMENTS:

1. Remove all Replit-specific configurations:
   - Delete:
     .replit
     replit.nix
     .replitignore
     any Replit-only files

2. Remove pnpm workspace:
   - Delete pnpm-workspace.yaml
   - Convert project to standard npm
   - Use a single root package.json
   - Ensure compatibility with:
       npm install
       npm start

3. Simplify and restructure project:
   - Flatten folder structure if needed
   - Remove unnecessary folders and dependencies
   - Create a clean structure:

     /project-root
       ├── server.js (or index.js)
       ├── package.json
       ├── public/
       ├── routes/ (optional)
       ├── .env.example
       └── README.md

4. Backend:
   - Use Express.js (lightweight setup)
   - Create a simple server entry file:
       server.js or index.js
   - Use:

       const PORT = process.env.PORT || 3000;

   - Ensure server runs without errors

5. Frontend:
   - Convert frontend to static HTML/CSS/JS if possible
   - Place files inside /public folder
   - Ensure Express serves static content:

       app.use(express.static("public"))

   - Ensure frontend works fully as static site

6. TypeScript:
   - Convert all code to JavaScript
   - Remove tsconfig.json
   - Remove build step if possible

7. Dependencies:
   - Keep only essential dependencies
   - Remove unsupported packages
   - Replace heavy or complex tools with simpler alternatives

8. Create Dual Mode Compatibility:

   A) Node Hosting Mode:
      - Runs with:
          npm install
          npm start

   B) Static Hosting Mode (Fallback):
      - Ensure /public folder works independently
      - Website loads even without backend (if possible)

9. package.json must include:

   {
     "scripts": {
       "start": "node server.js"
     }
   }

10. Environment Handling:
   - Create .env.example
   - Handle missing env variables safely
   - Do not crash if env variables are missing

11. Fix all issues:
   - routing issues
   - API connection issues
   - broken links
   - server errors

12. Optimize for production:
   - remove unused code
   - ensure fast loading
   - ensure minimal dependencies

13. Hosting compatibility:

   Ensure final project works on:
   - Render 
   - Railway 
   - VPS 
   - cPanel Node.js 
   - Shared hosting (static mode) 

14. Output required:

   Provide full working project including:
   - server.js
   - package.json
   - public folder
   - .env.example
   - README.md with deployment instructions

15. Deployment Instructions (IMPORTANT):

Explain clearly how to deploy this project on:

1) Render
   - steps to create a web service
   - build command
   - start command
   - environment variables

2) cPanel Node.js
   - how to upload files
   - how to set entry point
   - how to run npm install
   - how to start the app

3) Shared Hosting (Static Mode)
   - which folder to upload
   - how to make the site work without backend
   - how to use the /public folder

FINAL VALIDATION:

Before finishing:
   - test npm install
   - test npm start
   - confirm app runs successfully
   - confirm no missing files
   - confirm no runtime errors
   - confirm project deploys without manual fixes